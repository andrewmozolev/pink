mixin contacts
  +b.contacts
    +e.H2.headling Остались вопросы? Свяжитесь с нами!
    each item in site.data.contacts
      +e.item
        +e.H3.title= item.title + ":"
        case item.type
          when "phone"
            +e.A.description(href="tel:" + item.value)= item.description
          when "email"
            +e.A.description(href="mailto:" + item.value)= item.description
          when "address"
            +e.P.description= item.description

