mixin social
  +b.social
    each item in site.data.social

      +e.A.link(
        href= item.link,
        title= item.name
      )

        = item.name

        +e.SVG.svg(class = "svg-" + item.name)
          use(xlink:href="img/svg-sprite.svg#" + item.name)

